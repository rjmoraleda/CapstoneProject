let express,dotenv,connectDB;_de1‍.w("express",[["default",["express"],function(v){express=v}]]);_de1‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_de1‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);




// Configuration
dotenv.config();
connectDB();
const app = express();

// Middlewarre
app.use(express.json());

app.get("/", (req, res) => {
   res.send("Server is running");
});

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => console.log(`Server is running on port ${PORT}`));
